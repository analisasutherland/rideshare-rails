<h2> Passengers!</h2>

<div>
  <%= paginate @passengers %>
</div>
<ul class="Trip_List">
  <% @passengers.each do |passenger| %>
  <li>
    ID: <%= link_to(passenger.id, passenger_path(passenger)) %>

    NAME: <%= link_to(passenger.name, passenger_path(passenger)) %>
  </li>
  <% end %>
</ul>
